# Roadmap изучения OpenXRay: от простого к сложному

> **Для кого:** Сеньор React разработчик, изучающий C++ через игровой движок  
> **Цель:** Систематическое изучение всех архитектурных фич проекта

---

## 📋 Уровень 0: Подготовка (базовые концепции C++)

**Сложность:** ⭐  
**Время:** 1-2 дня  
**Цель:** Понять базовые паттерны, используемые в проекте

### 0.1. Управление памятью (Memory Management)
- [ ] **xrMemory** — кастомный аллокатор памяти
  - Файлы: `src/xrCore/xrMemory.h`, `src/xrCore/xrMemory.cpp`
  - Что изучить: `xr_new`, `xr_delete`, `mem_alloc`, `mem_free`
  - Аналогия: как `malloc/free`, но с кастомной логикой
  - Зачем: контроль над выделением памяти, оптимизация

- [ ] **Умные указатели** (Smart Pointers)
  - Файлы: `src/xrCommon/xr_smart_pointers.h`, `src/xrCore/intrusive_ptr.h`
  - Что изучить: `xr_unique_ptr`, `xr_shared_ptr`, `intrusive_ptr`
  - Аналогия: как `useRef`, `useState` в React — автоматическое управление жизнью
  - Зачем: безопасность памяти, автоматическая очистка

- [ ] **Reference counting** (ref_sound, ref_light)
  - Файлы: `src/xrCore/xr_resource.h`
  - Что изучить: как работает подсчёт ссылок
  - Аналогия: как React отслеживает зависимости
  - Зачем: разделение ресурсов между объектами

### 0.2. Контейнеры данных (Data Structures)
- [ ] **xr_vector, xr_list, xr_map**
  - Файлы: `src/xrCommon/xr_vector.h`, `src/xrCommon/xr_list.h`, `src/xrCommon/xr_map.h`
  - Что изучить: кастомные обёртки над STL
  - Аналогия: как массивы/объекты в JS, но типизированные
  - Зачем: контроль над аллокацией, оптимизация

- [ ] **shared_str** — строки с подсчётом ссылок
  - Файлы: `src/xrCore/xrstring.h`
  - Что изучить: как работает `shared_str`
  - Аналогия: как строки в JS (immutable), но с явным подсчётом ссылок
  - Зачем: эффективное хранение строк, избежание копий

### 0.3. Базовые типы и утилиты
- [ ] **Математические типы** (Fvector, Fmatrix, Frect)
  - Файлы: `src/xrCore/_vector3d.h`, `src/xrCore/_matrix.h`, `src/xrCore/_rect.h`
  - Что изучить: векторы, матрицы, прямоугольники
  - Аналогия: как типы для работы с координатами в Canvas API
  - Зачем: 3D математика, геометрия

---

## 📋 Уровень 1: Архитектурные паттерны (Design Patterns)

**Сложность:** ⭐⭐  
**Время:** 3-5 дней  
**Цель:** Понять основные паттерны проектирования

### 1.1. Observer Pattern (MessageRegistry)
- [ ] **Система событий и последовательностей**
  - Файлы: `src/xrEngine/pure.h`, `src/xrEngine/device.h` (seqFrame, seqRender)
  - Что изучить: `MessageRegistry`, `pureFrame`, `pureRender`
  - Аналогия: как EventEmitter в Node.js или Context API в React
  - Зачем: слабая связанность, подписка на события кадра/рендеринга
  - **Практика:** Найти все места, где объекты регистрируются в seqFrame

### 1.2. Factory Pattern
- [ ] **Фабрика объектов** (FactoryObjectBase)
  - Файлы: `src/xrServerEntities/object_factory.h`
  - Что изучить: как создаются игровые объекты
  - Аналогия: как фабричные функции в JS
  - Зачем: централизованное создание объектов по типу

### 1.3. Component Pattern
- [ ] **Базовые компоненты объектов**
  - Файлы: `src/xrGame/GameObject.h`
  - Что изучить: `SpatialBase`, `ScheduledBase`, `RenderableBase`, `CollidableBase`
  - Аналогия: как компоненты в React (композиция вместо наследования)
  - Зачем: модульность, переиспользование кода

### 1.4. Singleton Pattern
- [ ] **Глобальные менеджеры** (Engine, Device, UI)
  - Файлы: `src/xrEngine/Engine.h`, `src/xrEngine/device.h`
  - Что изучить: `GEnv`, глобальные экземпляры
  - Аналогия: как глобальные контексты в React
  - Зачем: единая точка доступа к системам

---

## 📋 Уровень 2: Игровой цикл (Game Loop)

**Сложность:** ⭐⭐  
**Время:** 2-3 дня  
**Цель:** Понять, как работает основной цикл игры

### Уровень 1: Общая картина (контекст)

- [ ] **1.1. Объяснить, что такое игровой цикл**
  - Бесконечный цикл, который выполняется пока игра работает
  - Каждая итерация = один кадр (frame)
  - Цель: обновить логику → отрисовать → повторить

- [ ] **1.2. Показать точку входа**
  - `CApplication::Run()` — главный цикл приложения
  - Файл: `src/xrEngine/x_ray.cpp:367`
  - `while (!SDL_QuitRequested())` — работает пока не закрыли окно

### Уровень 2: Структура одного кадра (что происходит за итерацию)

- [ ] **2.1. Обработка событий (Events)**
  - События SDL (окно, мышь, клавиатура)
  - `SDL_PeepEvents()` — получение событий
  - `Device.ProcessEvent()` — обработка каждого события
  - Примеры: изменение размера окна, фокус, закрытие

- [ ] **2.2. Обработка кадра (ProcessFrame)**
  - `Device.ProcessFrame()` — основной метод кадра
  - Файл: `src/xrEngine/device.cpp:261`
  - Внутри: обновление → рендеринг → синхронизация

### Уровень 3: Обновление логики (Update Phase)

- [ ] **3.1. Обновление времени (FrameMove)**
  - `FrameMove()` — подготовка времени для кадра
  - `dwFrame++` — счётчик кадров
  - `fTimeDelta` — время между кадрами (для физики/анимаций)
  - `dwTimeGlobal` — глобальное время игры
  - Режимы: постоянный FPS или переменный

- [ ] **3.2. Система последовательностей (MessageRegistry)**
  - Что это: список объектов, которые нужно обновить
  - Приоритеты: HIGH → NORMAL → LOW
  - `seqFrame.Process()` — вызывает `OnFrame()` у всех зарегистрированных объектов
  - Аналогия: очередь задач с приоритетами

- [ ] **3.3. Что обновляется в seqFrame**
  - `CEngine::OnFrame()` — события движка (приоритет HIGH+1000)
  - `IGame_Persistent::OnFrame()` — персистентная игровая логика (HIGH+1)
  - `IGame_Level::OnFrame()` — уровень и объекты
  - `CSheduler::Update()` — планировщик игровых объектов
  - `CDialogHolder::OnFrame()` — UI диалоги
  - Порядок: по приоритету (от высокого к низкому)

### Уровень 4: Планировщик (Sheduler) — как обновляются объекты

- [ ] **4.1. Что такое Sheduler**
  - Планировщик обновления игровых объектов
  - Два типа: Realtime (каждый кадр) и Scheduled (по расписанию)
  - `Objects.Update()` — обновление всех объектов уровня

- [ ] **4.2. Как работает обновление объектов**
  - Каждый объект реализует `shedule_Update(dt)`
  - `dt` — время с последнего обновления
  - Объекты могут регистрироваться/отменяться динамически

### Уровень 5: Рендеринг (Render Phase)

- [ ] **5.1. Когда происходит рендеринг**
  - После обновления логики
  - `DoRender()` — метод рендеринга
  - `seqRender.Process()` — вызывает `OnRender()` у всех зарегистрированных

- [ ] **5.2. Что рендерится**
  - `IGame_Level::OnRender()` — рендеринг уровня
  - `GEnv.Render->Calculate()` — расчёт видимости
  - `GEnv.Render->Render()` — отрисовка
  - ImGui — отладочный интерфейс
  - `RenderEnd()` — Present (показ кадра на экране)

### Уровень 6: Многопоточность (Parallel Processing)

- [ ] **6.1. Параллельные задачи**
  - `seqParallel` — список задач для параллельного выполнения
  - `seqFrameMT` — многопоточное обновление (например, звук)
  - Запуск через `TaskScheduler->AddTask()`
  - Ожидание через `TaskScheduler->Wait()`

- [ ] **6.2. Зачем это нужно**
  - Основной поток: логика + рендеринг
  - Фоновые потоки: звук, физика, загрузка
  - Улучшает производительность

### Уровень 7: Синхронизация и ограничение FPS

- [ ] **7.1. Контроль частоты кадров**
  - `ps_fps_limit` — лимит FPS
  - `ps_fps_limit_in_menu` — лимит в меню
  - Если кадр быстрый → `Sleep()` для синхронизации

- [ ] **7.2. Режимы работы**
  - Обычный режим: переменный FPS
  - Постоянный FPS: `rsConstantFPS` (30 или 50 FPS)
  - Пауза: `fTimeDelta = 0.0f`

### Уровень 8: Детали реализации (для углубления)

- [ ] **8.1. MessageRegistry — как работает**
  - Хранит список объектов с приоритетами
  - `Add()` — регистрация объекта
  - `Process()` — вызов всех `OnPure()` по приоритету
  - `Remove()` — удаление (помечает как INVALID)

- [ ] **8.2. Интерфейсы pureFrame, pureRender**
  - `pureFrame` — объекты, обновляющиеся каждый кадр
  - `pureRender` — объекты, которые рендерятся
  - Объект может быть и тем, и другим

- [ ] **8.3. Жизненный цикл объектов**
  - Регистрация: `Device.seqFrame.Add(this, priority)`
  - Обновление: автоматически каждый кадр
  - Удаление: `Device.seqFrame.Remove(this)`

### Уровень 9: Практические примеры

- [ ] **9.1. Пример: как объект попадает в цикл**
  - Создание объекта → регистрация в seqFrame → автоматическое обновление
  - Пример: `IGame_Level` регистрируется при загрузке уровня

- [ ] **9.2. Пример: порядок обновления**
  - Engine (события) → GamePersistent → Level → Objects → UI
  - Рендеринг: Level → UI → Debug

- [ ] **9.3. Пример: обработка паузы**
  - `Device.Paused()` → `fTimeDelta = 0.0f`
  - Обновление продолжается, но время не идёт
  - Рендеринг продолжается (видим застывшую картинку)

### Уровень 10: Визуализация потока данных

- [ ] **10.1. Схема одного кадра**
  ```
  SDL Events → ProcessEvent()
       ↓
  ProcessFrame()
       ↓
  FrameMove() → seqFrame.Process() → [Engine → GamePersistent → Level → Sheduler → UI]
       ↓
  OnCameraUpdated()
       ↓
  [Параллельно] seqParallel + seqFrameMT
       ↓
  DoRender() → seqRender.Process() → [Level Render → UI Render → ImGui]
       ↓
  Wait(parallel tasks)
       ↓
  Sleep(если нужно ограничить FPS)
  ```

- [ ] **10.2. Ключевые файлы для изучения**
  - `src/xrEngine/x_ray.cpp:367` — главный цикл
  - `src/xrEngine/device.cpp:261` — ProcessFrame
  - `src/xrEngine/device.cpp:433` — FrameMove
  - `src/xrEngine/pure.h:28` — MessageRegistry
  - `src/xrEngine/xrSheduler.cpp:425` — Sheduler

---

## 📋 Уровень 3: UI система (User Interface)

**Сложность:** ⭐⭐⭐  
**Время:** 4-6 дней  
**Цель:** Понять архитектуру UI

### 3.1. Базовые компоненты UI
- [ ] **CUIWindow** — базовый класс всех UI элементов
  - Файлы: `src/xrUICore/Windows/UIWindow.h`
  - Что изучить: иерархия окон, позиционирование, события
  - Аналогия: как DOM элементы в браузере
  - Зачем: все UI элементы наследуются от этого класса

### 3.2. Система сообщений UI
- [ ] **EUIMessages и CUIWndCallback**
  - Файлы: `src/xrUICore/UIMessages.h`, `src/xrUICore/Callbacks/UIWndCallback.h`
  - Что изучить: как обрабатываются события UI
  - Аналогия: как обработчики событий в React (onClick, onChange)
  - Зачем: реакция на действия пользователя

### 3.3. XML инициализация
- [ ] **CUIXmlInitBase** — загрузка UI из XML
  - Файлы: `src/xrUICore/XML/UIXmlInitBase.h`
  - Что изучить: как UI создаётся из XML файлов
  - Аналогия: как JSX в React (декларативное описание)
  - Зачем: отделение дизайна от логики

### 3.4. Система фокуса
- [ ] **CUIFocusSystem** — навигация по UI
  - Файлы: `src/xrUICore/ui_focus.h`
  - Что изучить: как работает фокус для клавиатуры/геймпада
  - Аналогия: как tabindex в HTML
  - Зачем: доступность, навигация без мыши

### 3.5. Диалоги и меню
- [ ] **CDialogHolder** — управление диалогами
  - Файлы: `src/xrGame/UIDialogHolder.h`
  - Что изучить: как показываются/скрываются диалоги
  - Аналогия: как модальные окна в React
  - Зачем: управление состоянием UI

---

## 📋 Уровень 4: Игровые объекты (Game Objects)

**Сложность:** ⭐⭐⭐  
**Время:** 5-7 дней  
**Цель:** Понять, как устроены игровые объекты

### 4.1. Базовый игровой объект
- [ ] **CGameObject** — основа всех игровых объектов
  - Файлы: `src/xrGame/GameObject.h`
  - Что изучить: компоненты, обновление, рендеринг
  - Аналогия: как Entity Component System (ECS)
  - Зачем: единая база для всех объектов игры

### 4.2. Жизненный цикл объектов
- [ ] **Создание, обновление, удаление**
  - Файлы: `src/xrGame/Level.cpp` (Objects.Load, Objects.Update)
  - Что изучить: как объекты появляются и исчезают
  - Аналогия: как mount/unmount в React
  - Зачем: управление ресурсами

### 4.3. Пространственная система
- [ ] **ISpatial и пространственные запросы**
  - Файлы: `src/xrCDB/ISpatial.h`
  - Что изучить: как объекты находятся в пространстве
  - Аналогия: как spatial indexing в базах данных
  - Зачем: быстрый поиск объектов по позиции

### 4.4. Списки объектов
- [ ] **CObjectList** — управление списками объектов
  - Файлы: `src/xrEngine/xr_object_list.h`
  - Что изучить: как объекты группируются и обновляются
  - Аналогия: как массивы объектов в React state
  - Зачем: эффективное обновление множества объектов

---

## 📋 Уровень 5: Физика (Physics)

**Сложность:** ⭐⭐⭐⭐  
**Время:** 7-10 дней  
**Цель:** Понять физический движок

### 5.1. Физический мир
- [ ] **CPHWorld** — физический мир
  - Файлы: `src/xrPhysics/PHWorld.h`
  - Что изучить: как создаётся и обновляется физический мир
  - Аналогия: как физический движок в Unity/Unreal
  - Зачем: симуляция физики (гравитация, столкновения)

### 5.2. Физические оболочки
- [ ] **CPhysicsShell** — физические тела
  - Файлы: `src/xrPhysics/PHShell.h`
  - Что изучить: как объекты взаимодействуют с физикой
  - Аналогия: как Rigidbody в Unity
  - Зачем: физическое поведение объектов

### 5.3. Коллизии
- [ ] **xrCDB** — система коллизий
  - Файлы: `src/xrCDB/xrCDB.h`
  - Что изучить: как определяются столкновения
  - Аналогия: как collision detection в играх
  - Зачем: определение пересечений объектов

### 5.4. Движение
- [ ] **CPHMovementControl** — управление движением
  - Файлы: `src/xrGame/movement_manager.h`
  - Что изучить: как персонажи двигаются
  - Аналогия: как character controller
  - Зачем: реалистичное движение персонажей

---

## 📋 Уровень 6: Рендеринг (Rendering)

**Сложность:** ⭐⭐⭐⭐⭐  
**Время:** 10-15 дней  
**Цель:** Понять систему рендеринга

### 6.1. Архитектура рендереров
- [ ] **IRender и модули рендеринга**
  - Файлы: `src/Layers/xrRender/`, `Include/xrRender/`
  - Что изучить: как устроена система рендеринга
  - Аналогия: как WebGL/Canvas API, но более низкоуровнево
  - Зачем: отрисовка графики

### 6.2. Камеры
- [ ] **CCameraManager** — управление камерами
  - Файлы: `src/xrEngine/CameraManager.h`
  - Что изучить: как работает камера
  - Аналогия: как viewport в браузере
  - Зачем: определение того, что видит игрок

### 6.3. Материалы и шейдеры
- [ ] **Система материалов**
  - Файлы: `src/xrMaterialSystem/`
  - Что изучить: как работают материалы
  - Аналогия: как CSS стили, но для 3D объектов
  - Зачем: внешний вид объектов

### 6.4. Освещение
- [ ] **Система освещения**
  - Файлы: `src/Layers/xrRender/`
  - Что изучить: как работает освещение
  - Аналогия: как CSS box-shadow, но в 3D
  - Зачем: реалистичное освещение сцены

### 6.5. Частицы
- [ ] **Система частиц**
  - Файлы: `src/xrParticles/`
  - Что изучить: как создаются эффекты (дым, огонь)
  - Аналогия: как анимации в CSS, но для частиц
  - Зачем: визуальные эффекты

---

## 📋 Уровень 7: Звук (Audio)

**Сложность:** ⭐⭐⭐  
**Время:** 3-5 дней  
**Цель:** Понять звуковую систему

### 7.1. Звуковой менеджер
- [ ] **CSoundManager** — управление звуком
  - Файлы: `src/xrSound/Sound.h`
  - Что изучить: как загружаются и воспроизводятся звуки
  - Аналогия: как Audio API в браузере
  - Зачем: звуковые эффекты и музыка

### 7.2. 3D звук
- [ ] **Пространственный звук**
  - Файлы: `src/xrSound/SoundRender_Scene.h`
  - Что изучить: как звук позиционируется в 3D
  - Аналогия: как Web Audio API с позиционированием
  - Зачем: реалистичный звук в 3D пространстве

---

## 📋 Уровень 8: Скриптовый движок (Scripting)

**Сложность:** ⭐⭐⭐⭐  
**Время:** 7-10 дней  
**Цель:** Понять интеграцию Lua

### 8.1. Lua интеграция
- [ ] **xrScriptEngine** — скриптовый движок
  - Файлы: `src/xrScriptEngine/`
  - Что изучить: как C++ взаимодействует с Lua
  - Аналогия: как Node.js с нативными модулями
  - Зачем: игровая логика на скриптах

### 8.2. Биндинги
- [ ] **Связывание C++ и Lua**
  - Файлы: `src/xrGame/script_*.cpp`
  - Что изучить: как C++ классы экспортируются в Lua
  - Аналогия: как FFI (Foreign Function Interface)
  - Зачем: доступ к движку из скриптов

### 8.3. Игровые скрипты
- [ ] **Использование скриптов в игре**
  - Файлы: `src/xrGame/script_*.cpp`
  - Что изучить: как скрипты используются для логики
  - Аналогия: как скрипты в браузере (JS)
  - Зачем: гибкость, модифицируемость

---

## 📋 Уровень 9: Сеть (Networking)

**Сложность:** ⭐⭐⭐⭐⭐  
**Время:** 10-15 дней  
**Цель:** Понять сетевую архитектуру

### 9.1. Сетевой протокол
- [ ] **xrNetServer** — сетевая подсистема
  - Файлы: `src/xrNetServer/`
  - Что изучить: как работает сетевой протокол
  - Аналогия: как WebSocket, но для игр
  - Зачем: мультиплеер

### 9.2. Клиент-сервер
- [ ] **NET_Client и NET_Server**
  - Файлы: `src/xrNetServer/NET_Client.h`, `src/xrNetServer/NET_Server.h`
  - Что изучить: как клиент и сервер общаются
  - Аналогия: как REST API, но в реальном времени
  - Зачем: синхронизация состояния игры

### 9.3. Синхронизация
- [ ] **Синхронизация объектов**
  - Файлы: `src/xrGame/Level.cpp` (ClientReceive)
  - Что изучить: как синхронизируются объекты
  - Аналогия: как state management в React (Redux)
  - Зачем: одинаковое состояние у всех игроков

---

## 📋 Уровень 10: Искусственный интеллект (AI)

**Сложность:** ⭐⭐⭐⭐⭐  
**Время:** 15-20 дней  
**Цель:** Понять AI систему

### 10.1. AI пространство
- [ ] **CAISpace** — AI система
  - Файлы: `src/xrAICore/`
  - Что изучить: как работает AI
  - Аналогия: как state machine в React
  - Зачем: поведение NPC

### 10.2. Навигация
- [ ] **Система навигации**
  - Файлы: `src/xrAICore/Navigation/`
  - Что изучить: как NPC находят путь
  - Аналогия: как pathfinding алгоритмы (A*)
  - Зачем: движение NPC по карте

### 10.3. Планировщик действий
- [ ] **CAgentManagerPlanner** — планирование действий
  - Файлы: `src/xrGame/agent_manager.h`
  - Что изучить: как NPC принимают решения
  - Аналогия: как decision tree
  - Зачем: интеллектуальное поведение

### 10.4. Восприятие
- [ ] **Система зрения и слуха**
  - Файлы: `src/xrGame/vision_client.h`
  - Что изучить: как NPC видят и слышат
  - Аналогия: как система событий
  - Зачем: реакция на игрока

---

## 📋 Уровень 11: Файловая система (File System)

**Сложность:** ⭐⭐  
**Время:** 2-3 дня  
**Цель:** Понять работу с файлами

### 11.1. Виртуальная файловая система
- [ ] **LocatorAPI** — файловая система
  - Файлы: `src/xrCore/LocatorAPI.h`
  - Что изучить: как загружаются ресурсы
  - Аналогия: как require/import в Node.js
  - Зачем: работа с архивами, модами

### 11.2. Загрузка ресурсов
- [ ] **Загрузка текстур, моделей, звуков**
  - Файлы: `src/xrCore/FS.h`
  - Что изучить: как ресурсы загружаются из файлов
  - Аналогия: как asset loading в веб-приложениях
  - Зачем: доступ к игровым ресурсам

---

## 📋 Уровень 12: События и сообщения (Events)

**Сложность:** ⭐⭐⭐  
**Время:** 3-5 дней  
**Цель:** Понять систему событий

### 12.1. Event API
- [ ] **CEventAPI** — система событий
  - Файлы: `src/xrEngine/EventAPI.h`
  - Что изучить: как работают события движка
  - Аналогия: как EventEmitter в Node.js
  - Зачем: слабая связанность систем

### 12.2. Игровые события
- [ ] **События игры**
  - Файлы: `src/xrGame/`
  - Что изучить: какие события есть в игре
  - Аналогия: как события DOM
  - Зачем: реакция на игровые ситуации

---

## 📋 Уровень 13: Инвентарь и предметы (Inventory)

**Сложность:** ⭐⭐⭐  
**Время:** 5-7 дней  
**Цель:** Понять систему инвентаря

### 13.1. Предметы
- [ ] **CInventoryItem** — базовый класс предметов
  - Файлы: `src/xrGame/inventory_item.h`
  - Что изучить: как устроены предметы
  - Аналогия: как объекты данных
  - Зачем: хранение информации о предметах

### 13.2. Инвентарь
- [ ] **CInventory** — система инвентаря
  - Файлы: `src/xrGame/inventory.h`
  - Что изучить: как предметы хранятся
  - Аналогия: как массив объектов в React state
  - Зачем: управление предметами игрока

### 13.3. UI инвентаря
- [ ] **UIActorMenu** — интерфейс инвентаря
  - Файлы: `src/xrGame/ui/UIActorMenu.h`
  - Что изучить: как показывается инвентарь
  - Аналогия: как компонент списка в React
  - Зачем: взаимодействие с инвентарём

---

## 📋 Уровень 14: Продвинутые темы

**Сложность:** ⭐⭐⭐⭐⭐  
**Время:** 20+ дней  
**Цель:** Глубокое понимание движка

### 14.1. Многопоточность
- [ ] **TaskManager и параллельные задачи**
  - Файлы: `src/xrCore/Threading/TaskManager.hpp`
  - Что изучить: как работает многопоточность
  - Аналогия: как Web Workers в браузере
  - Зачем: производительность

### 14.2. Оптимизация
- [ ] **Профилирование и оптимизация**
  - Файлы: `src/xrEngine/profiler.h`
  - Что изучить: как измеряется производительность
  - Аналогия: как React DevTools Profiler
  - Зачем: улучшение FPS

### 14.3. Моды и расширяемость
- [ ] **Система модов**
  - Файлы: `src/xrCore/FS.h` (работа с модами)
  - Что изучить: как работают моды
  - Аналогия: как плагины в веб-приложениях
  - Зачем: расширяемость игры

---

## 🎯 Рекомендации по изучению

### Для React разработчика:

1. **Начни с Уровня 0-2** — это база, без неё будет сложно
2. **Используй аналогии с React:**
   - `MessageRegistry` ≈ Context API
   - `CUIWindow` ≈ React компоненты
   - `seqFrame` ≈ useEffect
   - `GameObject` ≈ Entity Component System

3. **Практикуйся:**
   - Читай код и пытайся понять логику
   - Делай небольшие изменения
   - Используй отладчик (GDB/LLDB)

4. **Изучай постепенно:**
   - Не пытайся понять всё сразу
   - Возвращайся к предыдущим уровням
   - Делай заметки

5. **Используй инструменты:**
   - IDE с хорошей навигацией (CLion, VS Code)
   - Поиск по коду (grep, ripgrep)
   - Отладчик для понимания потока выполнения

### Порядок изучения (рекомендуемый):

```
Уровень 0 → Уровень 1 → Уровень 2 → Уровень 3 → Уровень 4
    ↓
Уровень 11 → Уровень 12 (для понимания загрузки)
    ↓
Уровень 5 → Уровень 6 → Уровень 7 (основные системы)
    ↓
Уровень 8 → Уровень 13 (скрипты и игровая логика)
    ↓
Уровень 9 → Уровень 10 (продвинутые системы)
    ↓
Уровень 14 (оптимизация и продвинутые темы)
```

---

## 📚 Дополнительные ресурсы

- **Документация проекта:** `doc/` папка
- **Примеры кода:** Ищи `example`, `test`, `demo` в названиях файлов
- **Комментарии в коде:** Много полезной информации в комментариях
- **GitHub Issues:** Полезно для понимания проблем и решений

---

## ✅ Чек-лист прогресса

Отмечай пройденные темы:

- [ ] Уровень 0: Подготовка
- [ ] Уровень 1: Архитектурные паттерны
- [ ] Уровень 2: Игровой цикл
- [ ] Уровень 3: UI система
- [ ] Уровень 4: Игровые объекты
- [ ] Уровень 5: Физика
- [ ] Уровень 6: Рендеринг
- [ ] Уровень 7: Звук
- [ ] Уровень 8: Скриптовый движок
- [ ] Уровень 9: Сеть
- [ ] Уровень 10: AI
- [ ] Уровень 11: Файловая система
- [ ] Уровень 12: События
- [ ] Уровень 13: Инвентарь
- [ ] Уровень 14: Продвинутые темы

---

**Удачи в изучении! 🚀**

